apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv_volume_01
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data"
